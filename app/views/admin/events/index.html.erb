<% @page_title = human_model_name.humanize.pluralize %>

<%= partial 'admin/shared/collection_option_links' %>

<table class="index">
  <tr>
	<th>Title</th>
	<th>Image</th>
	<th>Intro</th>
	<th></th>
  </tr>
  <% @events.each do |event| %>
  <tr>
	<td><%= event.title %></td>
	<td><%= image_tag event.image.url(:thumbnail) %></td>
	<td><%= event.intro %></td>
	<td class="actions" align="right">
      <%= link_to_with_icon 'edit', 'Edit', edit_object_url(event) %> &nbsp;
      <%= link_to "#{image_tag('/images/admin/delete.gif')} Delete", object_url(event), :method => :delete, :confirm => 'Are you sure?' %> &nbsp; 
    </td>
  </tr>
  <% end %>
</table>